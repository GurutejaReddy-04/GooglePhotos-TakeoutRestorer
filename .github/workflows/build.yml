name: Build Windows Executable

on:
  release:
    types: [created]
  workflow_dispatch:  # Allow manual triggering from the Actions tab

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build with PyInstaller
      run: |
        pyinstaller GooglePhotosTakeoutRestorer.spec --clean -y

    - name: Zip the build folder
      run: |
        Compress-Archive -Path dist\GooglePhotosTakeoutRestorer -DestinationPath GooglePhotosTakeoutRestorer.zip

    - name: Upload artifact to workflow run
      uses: actions/upload-artifact@v4
      with:
        name: GooglePhotosTakeoutRestorer
        path: GooglePhotosTakeoutRestorer.zip

    - name: Upload artifact to release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: GooglePhotosTakeoutRestorer.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
